# Paper-faithful stack. Needs ~4 GB of model downloads + a GPU for the LLM.
torch>=2.2
sentence-transformers>=3.0        # paraphrase-multilingual-mpnet-base-v2, cross-encoder
faiss-cpu>=1.8                    # HNSW index  (use faiss-gpu if you have CUDA)
rank_bm25>=0.2.2                  # reference BM25 (we also ship our own)
transformers>=4.40
accelerate>=0.30
bert-score>=0.3.13                # BERTScore F1 (Table 4)
rouge-score>=0.1.2
mlflow>=2.12                      # experiment tracking
statsmodels>=0.14                 # ANCOVA cross-check
openai>=1.30                      # GPT-4o-mini fallback generator
indic-transliteration>=2.3        # higher-quality Roman -> Devanagari
