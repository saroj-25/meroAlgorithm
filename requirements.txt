# ---------------------------------------------------------------------------
# CORE  (required - the whole project runs offline with only these)
# ---------------------------------------------------------------------------
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.7
plotly>=5.18
PyYAML>=6.0
tqdm>=4.66
streamlit>=1.32
pytest>=7.4

# ---------------------------------------------------------------------------
# FULL-FIDELITY STACK  (optional - enables the exact models used in the paper)
# Install with:  pip install -r requirements.txt -r requirements-full.txt
# ---------------------------------------------------------------------------
# see requirements-full.txt
